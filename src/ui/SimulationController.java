package ui;
import javax.swing.*;
import java.awt.*;

public class SimulationController extends JPanel {
    JButton startButton = new JButton("Start");
    JButton stopButton = new JButton("Stop");
    JSlider speedSlider = new JSlider(JSlider.HORIZONTAL, 0, 1000);


}
